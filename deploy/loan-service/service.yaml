apiVersion: v1
kind: Service
metadata:
  name: loan-service
  namespace: libeasy
spec:
  selector:
    app: loan-service
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  type: ClusterIP

