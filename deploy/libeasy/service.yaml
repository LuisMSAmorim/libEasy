apiVersion: v1
kind: Service
metadata:
  name: libeasy
  namespace: libeasy
  labels:
    app: libeasy
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: libeasy

